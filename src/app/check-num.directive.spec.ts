import { CheckNumDirective } from './check-num.directive';

describe('CheckNumDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckNumDirective();
    expect(directive).toBeTruthy();
  });
});
